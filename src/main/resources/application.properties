# ---------- Datasource ----------
spring.datasource.url=jdbc:postgresql://localhost:5432/library
spring.datasource.username=postgres
spring.datasource.password=wan33ame
spring.datasource.driver-class-name=org.postgresql.Driver

# ---------- SQL init ----------
# Ne pas utiliser spring.sql.init.mode=always sur Postgres en prod;
spring.sql.init.mode=never

# ---------- JPA / Hibernate ----------
spring.jpa.hibernate.ddl-auto=update   # dev: update ; prod: validate or none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=UTC=30000

